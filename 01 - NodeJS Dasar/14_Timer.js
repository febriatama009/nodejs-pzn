//The timer module exposes a global API for scheduling functions to be called at some future period of time.

//karena default nya adalah callback, jadi jika ingin menggunakan Promises, library ini harus diimport

//Timers Promises API
import { setTimeout, setImmediate, setInterval } from "timers/promises";
